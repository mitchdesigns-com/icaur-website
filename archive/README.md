# Archive

Static HTML retained for reference only. Live routes render from `app/[locale]/**` + `components/views/**`.

| Folder | Contents |
|--------|----------|
| `content-pages/` | Former `content/pages/*.html` page shells |
| `harnesses/` | Root `_*.html` visual QA harnesses |
| `static-mirrors/` | Former root route folders + `css/` / `js/` / `dist/` mirrors |

Canonical CSS/JS: `public/css/**`, `public/js/**` only.
